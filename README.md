Run from the root like:

python -m sgx_parser "C:\Path\to\sgx.sgx" [--out outfolder_name]

or python -m pip install git+https://github.com/Greedquest/SGX-Reader.git
sgx-reader "C:\Path\to\sgx.sgx" [--out outfolder_name]
